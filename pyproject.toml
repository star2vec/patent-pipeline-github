[project]
name = "patent-pipeline"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "flask==3.0.0",
    "numpy",
    "pandas",
    "scikit-learn",
    "torch",
    "sentence-transformers",
    "tqdm",
    "torch_geometric",
    "networkx",
]

[tool.uv]
dev-dependencies = []
